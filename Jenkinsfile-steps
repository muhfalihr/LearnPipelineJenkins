pipeline {
    agent none
    stages {
        stage('Build') {
            steps {
                echo 'Build 1'
                echo 'Build 2'
                echo 'Build 3'
            }
        }
        stage('Test') {
            steps {
                echo 'Test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}